.DS_Store
*.xcuserstate
xcuserdata/
build/
DerivedData/

# Off-device cloud backend (Cloudflare Worker). NOT shipped: the app is
# on-device only and must make no network calls related to user data. Excluded
# so this prohibited infrastructure can never be committed/shipped with the app.
backend/

# Release artifacts (signed builds, appcast) produced by release.sh
dist/

# Landing site now lives in this repo under web/. Its build artifacts are
# ignored via web/.gitignore. Local screenshots in web/public/screenshots/ hold
# real disk data — kept untracked until replaced with shareable ones.
